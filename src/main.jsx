import React,{useEffect,useMemo,useRef,useState}from"react";
import{createRoot}from"react-dom/client";
import{Archive,BookOpen,Car,CheckCircle2,ClipboardList,Clock,Edit3,Euro,Eye,FileText,ImagePlus,LogOut,Plus,Printer,Save,Search,Send,Shield,Trash2,Users,X}from"lucide-react";
import"./styles.css";
import logo from"./assets/logo.png";
import hero from"./assets/dashboard-hero.jpeg";
import{hasSupabaseConfig,loadCloudState,saveCloudState,subscribeCloudState,supabase}from"./lib/supabase";

const ENTREPRISE={nom:"THE KING PIÈCES AUTOS",slogan:"Vente toutes marques",telFixe:"01 84 74 15 00",whatsapp:"06 50 05 89 45",email:"thekingpiecesautos@gmail.com",adresse:"32 AV MARCEL CACHIN, 93240 STAINS"};
const LS_KEY="tkpa_cahier_pro_v2",SESSION_KEY="tkpa_current_user_id",CLOTURE_HEURE=19;
const CAR_MODELS={Peugeot:["106","107","108","206","207","208","307","308","407","508","2008","3008","5008","Partner","Expert","Boxer"],Renault:["Clio","Megane","Scenic","Captur","Kadjar","Kangoo","Trafic","Master","Twingo","Laguna","Espace"],Citroën:["C1","C2","C3","C4","C5","C3 Aircross","C4 Picasso","C5 Aircross","Berlingo","Jumpy","Jumper"],Volkswagen:["Polo","Golf","Passat","Touran","Tiguan","T-Roc","T-Cross","Caddy","Transporter","Crafter"],Audi:["A1","A3","A4","A5","A6","Q2","Q3","Q5","Q7","TT"],BMW:["Série 1","Série 2","Série 3","Série 5","X1","X3","X5"],"Mercedes-Benz":["Classe A","Classe B","Classe C","Classe E","CLA","GLA","GLC","Vito","Sprinter"],Ford:["Fiesta","Focus","Kuga","C-Max","Mondeo","Transit","Ranger"],Opel:["Corsa","Astra","Mokka","Insignia","Zafira","Vivaro","Movano"],Fiat:["500","Panda","Punto","Tipo","Doblo","Ducato"],Toyota:["Yaris","Auris","Corolla","RAV4","C-HR","Aygo","Hilux"],Nissan:["Micra","Juke","Qashqai","X-Trail","Note","NV200"],Dacia:["Sandero","Logan","Duster","Dokker","Jogger"],Autre:["Modèle à saisir manuellement"]};
const CAR_BRANDS=Object.keys(CAR_MODELS);
const initialUsers=[{id:"admin",nom:"Mokrane",identifiant:"mokrane",motDePasse:"admin",role:"admin"},{id:"poste1",nom:"Poste 1",identifiant:"poste1",motDePasse:"1234",role:"salarie"}];
function uid(){return`${Date.now()}-${Math.random().toString(16).slice(2)}`}
function today(){const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
function currentTime(){const d=new Date();return`${String(d.getHours()).padStart(2,"0")}:${String(d.getMinutes()).padStart(2,"0")}`}
function money(n){return`${Number(n||0).toFixed(2)} €`}
function htFromTtc(ttc){return Number(ttc||0)/1.2}
function tvaFromTtc(ttc){return Number(ttc||0)-htFromTtc(ttc)}
function splitPieces(t){return String(t||"").split(/\n|,|;/).map(x=>x.trim()).filter(Boolean)}
function normalizePlate(v=""){return String(v).toUpperCase().replace(/[^A-Z0-9]/g,"")}
function isPlateSearch(v=""){return normalizePlate(v).length>=5}
function isSameDay(d,day=today()){return String(d||"").slice(0,10)===day}
function propEmpty(n){return{id:uid(),numero:n,reference:"",marque:"",prix:"",note:"",image:"",selectionnee:n===1}}
function emptyPiece(nom=""){return{id:uid(),designation:nom,quantite:1,remarque:"",propositions:[propEmpty(1),propEmpty(2)]}}
function nextNum(items=[],prefix="FICHE"){const y=new Date().getFullYear();const nums=items.map(x=>String(x.numero||"")).map(n=>{const m=n.match(new RegExp(`${prefix}-${y}-(\\d+)`));return m?Number(m[1]):0});return`${prefix}-${y}-${String((nums.length?Math.max(...nums):0)+1).padStart(4,"0")}`}
function vehicleName(f){return[f.marqueManuelle||f.marque,f.modeleManuel||f.modele,f.finition].filter(Boolean).join(" ")}
function selectedProps(p){const props=p.propositions||[];const sel=props.filter(x=>x.selectionnee);return sel.length?sel:props.slice(0,1)}
function totalFiche(f){return(f.pieces||[]).reduce((s,p)=>s+selectedProps(p).reduce((a,pr)=>a+Number(pr.prix||0)*Number(p.quantite||1),0),0)}
function linesForDevis(f){return(f.pieces||[]).flatMap(p=>selectedProps(p).map(pr=>({id:`${p.id}-${pr.id}`,designation:p.designation,quantite:Number(p.quantite||1),prixTTC:Number(pr.prix||0)})))}
function totalDevis(d){return(d.lignes||[]).reduce((s,l)=>s+Number(l.quantite||1)*Number(l.prixTTC||0),0)}
function emptyFiche(user,all=[]){return{id:uid(),numero:nextNum(all,"FICHE"),date:today(),heureCreation:currentTime(),source:"sur_place",statut:"en_attente",enregistreCahier:false,envoyeDevis:false,clientNom:"",clientTelephone:"",immatriculation:"",vin:"",marque:"",modele:"",marqueManuelle:"",modeleManuel:"",finition:"",demandeRapide:"",remarque:"",creeParId:user?.id||"",creeParNom:user?.nom||"",pieces:[]}}
function emptyDevis(f,all=[]){return{id:uid(),ficheId:f.id,numero:nextNum(all,"DEVIS"),date:today(),heureCreation:currentTime(),clientNom:f.clientNom,clientTelephone:f.clientTelephone,immatriculation:f.immatriculation,vin:f.vin,vehicule:vehicleName(f),creeParId:f.creeParId,creeParNom:f.creeParNom,lignes:linesForDevis(f),remarque:f.remarque}}
function normalizeState(r){return{users:Array.isArray(r?.users)&&r.users.length?r.users:initialUsers,fiches:Array.isArray(r?.fiches)?r.fiches:[],devis:Array.isArray(r?.devis)?r.devis:[],archivesJour:Array.isArray(r?.archivesJour)?r.archivesJour:[],lastClosureDate:r?.lastClosureDate||""}}
function loadState(){try{const x=localStorage.getItem(LS_KEY);if(x)return normalizeState(JSON.parse(x))}catch{}return normalizeState({})}
function saveLocal(d){localStorage.setItem(LS_KEY,JSON.stringify(normalizeState(d)))}
async function saveEverywhere(d){const clean=normalizeState(d);saveLocal(clean);if(hasSupabaseConfig){try{await saveCloudState(clean)}catch(e){console.error("Supabase",e)}}}
function saveSession(u){localStorage.setItem(SESSION_KEY,JSON.stringify({id:u.id,identifiant:u.identifiant,nom:u.nom,role:u.role,motDePasse:u.motDePasse}))}
function loadSession(users=[]){try{const raw=localStorage.getItem(SESSION_KEY);if(!raw)return null;const s=JSON.parse(raw);return users.find(u=>u.id===s.id)||users.find(u=>u.identifiant===s.identifiant)||s}catch{return null}}
function clearSession(){localStorage.removeItem(SESSION_KEY)}
function buildArchive(data,date=today()){const fiches=data.fiches.filter(f=>f.date===date);const devis=data.devis.filter(d=>d.date===date);return{id:`ARCH-${date}`,date,createdAt:new Date().toISOString(),fiches,devis,resume:data.users.map(u=>({userId:u.id,nom:u.nom,fiches:fiches.filter(f=>f.creeParId===u.id).length,devis:devis.filter(d=>d.creeParId===u.id).length,total:devis.filter(d=>d.creeParId===u.id).reduce((s,d)=>s+totalDevis(d),0)}))}}
function closeDay(data,date=today()){const a=buildArchive(data,date);const exists=data.archivesJour.some(x=>x.date===date);return{...data,archivesJour:exists?data.archivesJour.map(x=>x.date===date?a:x):[a,...data.archivesJour],lastClosureDate:date,fiches:data.fiches.map(f=>f.date===date?{...f,archiveJourId:a.id}:f)}}
function Header({title,subtitle}){return <div className="header"><h1>{title}</h1><p>{subtitle}</p></div>}
function App(){
const[data,setData]=useState(()=>normalizeState(loadState()));const[login,setLogin]=useState({identifiant:"",motDePasse:""});const[currentUser,setCurrentUser]=useState(()=>loadSession(loadState().users));const[active,setActive]=useState("dashboard");const[selectedUserId,setSelectedUserId]=useState("all");const[search,setSearch]=useState("");const[editing,setEditing]=useState(null);const[openPieceId,setOpenPieceId]=useState("");const[preview,setPreview]=useState(null);const[userForm,setUserForm]=useState({nom:"",identifiant:"",motDePasse:"",role:"salarie"});const[syncStatus,setSyncStatus]=useState(hasSupabaseConfig?"Connexion Supabase...":"Mode local");const last=useRef(0);
useEffect(()=>{let cancel=false;async function start(){if(!hasSupabaseConfig){setSyncStatus("Mode local");return}try{const cloud=await loadCloudState();if(cancel)return;const clean=cloud?normalizeState(cloud):normalizeState(loadState());if(!cloud)await saveCloudState(clean);setData(clean);saveLocal(clean);const su=loadSession(clean.users);if(su)setCurrentUser(su);setSyncStatus("Synchronisé")}catch(e){console.error(e);setSyncStatus("Erreur Supabase")}}start();const ch=subscribeCloudState(n=>{if(Date.now()-last.current<800)return;const c=normalizeState(n);setData(c);saveLocal(c);const su=loadSession(c.users);if(su)setCurrentUser(su);setSyncStatus("Mise à jour reçue")});return()=>{cancel=true;if(ch&&supabase)supabase.removeChannel(ch)}} ,[]);
useEffect(()=>{if(currentUser)setSelectedUserId(currentUser.role==="admin"?"all":currentUser.id)},[currentUser?.id]);
function commit(n){const c=normalizeState(n);last.current=Date.now();setData(c);saveEverywhere(c);setSyncStatus(hasSupabaseConfig?"Sauvegardé et synchronisé":"Sauvegardé local")}
function connect(e){e.preventDefault();const u=data.users.find(x=>x.identifiant.toLowerCase()===login.identifiant.trim().toLowerCase()&&x.motDePasse===login.motDePasse);if(!u)return alert("Identifiant ou mot de passe incorrect.");setCurrentUser(u);saveSession(u)}
function newFiche(){const f=emptyFiche(currentUser,data.fiches);setEditing(f);setOpenPieceId("");setActive("edition")}
function saveFiche(fiche=editing){const f={...fiche,enregistreCahier:true};if(!f.demandeRapide&&!(f.pieces||[]).length&&!f.immatriculation&&!f.vin&&!f.clientNom)return alert("Ajoute une demande, une plaque, un VIN ou un nom.");const exists=data.fiches.some(x=>x.id===f.id);commit({...data,fiches:exists?data.fiches.map(x=>x.id===f.id?f:x):[f,...data.fiches]});setEditing(f);alert("Enregistré dans le cahier.")}
function sendToDevis(fiche=editing){const f={...fiche,statut:"realise",enregistreCahier:true,envoyeDevis:true};if(!linesForDevis(f).length)return alert("Ajoute au minimum une pièce avec un prix sélectionné.");const dev=emptyDevis(f,data.devis);const fiches=data.fiches.some(x=>x.id===f.id)?data.fiches.map(x=>x.id===f.id?f:x):[f,...data.fiches];commit({...data,fiches,devis:[dev,...data.devis]});setEditing(f);setActive("devis");alert("Envoyé dans Devis.")}
function canEdit(f){return currentUser?.role==="admin"||f.creeParId===currentUser?.id}function canDelete(){return currentUser?.role==="admin"}
const visibleFiches=useMemo(()=>{const q=search.trim().toLowerCase(),pq=normalizePlate(search),plateMode=currentUser?.role!=="admin"&&isPlateSearch(search);return data.fiches.filter(f=>{if(currentUser?.role==="admin")return selectedUserId==="all"||f.creeParId===selectedUserId;if(f.creeParId===currentUser?.id)return true;return plateMode&&normalizePlate(f.immatriculation).includes(pq)}).filter(f=>{if(!q)return true;if(currentUser?.role!=="admin"&&f.creeParId!==currentUser?.id)return normalizePlate(f.immatriculation).includes(pq);return[f.numero,f.clientNom,f.clientTelephone,f.immatriculation,f.vin,vehicleName(f),f.creeParNom,f.demandeRapide,...(f.pieces||[]).flatMap(p=>[p.designation,...(p.propositions||[]).flatMap(pr=>[pr.reference,pr.marque,pr.prix])])].join(" ").toLowerCase().includes(q)})},[data.fiches,search,selectedUserId,currentUser]);
const visibleDevis=useMemo(()=>{const q=search.trim().toLowerCase();return data.devis.filter(d=>currentUser?.role==="admin"||d.creeParId===currentUser?.id).filter(d=>!q||[d.numero,d.clientNom,d.clientTelephone,d.immatriculation,d.vin,d.vehicule,...(d.lignes||[]).map(l=>l.designation)].join(" ").toLowerCase().includes(q))},[data.devis,search,currentUser]);
if(!currentUser)return <div className="login-page"><div className="login-card"><div className="brand"><img src={logo}/><div><h1>{ENTREPRISE.nom}</h1><p>Cahier Pro synchronisé</p></div></div><form onSubmit={connect} className="login-form"><label>Identifiant<input value={login.identifiant} onChange={e=>setLogin({...login,identifiant:e.target.value})}/></label><label>Mot de passe<input type="password" value={login.motDePasse} onChange={e=>setLogin({...login,motDePasse:e.target.value})}/></label><button className="primary full">Connexion</button></form></div></div>;
const stats=data.users.map(u=>{const f=data.fiches.filter(x=>x.creeParId===u.id&&isSameDay(x.date));const d=data.devis.filter(x=>x.creeParId===u.id&&isSameDay(x.date));return{user:u,fiches:f.length,realise:f.filter(x=>x.statut==="realise").length,devis:d.length,total:d.reduce((s,x)=>s+totalDevis(x),0)}});
return <div className="app"><aside><div className="side-brand"><img src={logo}/><b>{ENTREPRISE.nom}</b><small>{currentUser.nom} · {currentUser.role==="admin"?"Administrateur":"Salarié"}</small><small className="sync-status">{syncStatus}</small></div><nav><button className={active==="dashboard"?"on":""} onClick={()=>setActive("dashboard")}><BookOpen/>Tableau de bord</button><button className={active==="cahier"?"on":""} onClick={()=>setActive("cahier")}><Archive/>Cahier Pro</button><button onClick={newFiche}><Plus/>Nouvelle demande</button><button className={active==="devis"?"on":""} onClick={()=>setActive("devis")}><FileText/>Devis</button><button className={active==="sauvegardes"?"on":""} onClick={()=>setActive("sauvegardes")}><ClipboardList/>Sauvegardes jour</button>{currentUser.role==="admin"&&<button className={active==="users"?"on":""} onClick={()=>setActive("users")}><Users/>Utilisateurs</button>}<button onClick={()=>{clearSession();setCurrentUser(null)}}><LogOut/>Déconnexion</button></nav></aside><main>{active==="dashboard"&&<Dashboard data={data} stats={stats} currentUser={currentUser} newFiche={newFiche} setEditing={setEditing} setOpenPieceId={setOpenPieceId} setActive={setActive}/>} {active==="cahier"&&<Cahier data={data} currentUser={currentUser} selectedUserId={selectedUserId} setSelectedUserId={setSelectedUserId} search={search} setSearch={setSearch} visibleFiches={visibleFiches} newFiche={newFiche} setPreview={setPreview} canEdit={canEdit} canDelete={canDelete} commit={commit} setEditing={setEditing} setOpenPieceId={setOpenPieceId} setActive={setActive}/>} {active==="edition"&&editing&&<Editor editing={editing} setEditing={setEditing} openPieceId={openPieceId} setOpenPieceId={setOpenPieceId} saveFiche={saveFiche} sendToDevis={sendToDevis} setPreview={setPreview} cancel={()=>setActive("cahier")}/>} {active==="devis"&&<DevisView visibleDevis={visibleDevis} search={search} setSearch={setSearch}/>} {active==="sauvegardes"&&<Sauvegardes data={data} commit={commit}/>} {active==="users"&&currentUser.role==="admin"&&<UsersView data={data} commit={commit} currentUser={currentUser} userForm={userForm} setUserForm={setUserForm}/>} {preview&&<PreviewModal fiche={preview} onClose={()=>setPreview(null)} sendToDevis={()=>sendToDevis(preview)}/>}</main></div>}
function Dashboard({data,stats,currentUser,newFiche,setEditing,setOpenPieceId,setActive}){return <section><div className="hero" style={{backgroundImage:`linear-gradient(90deg,rgba(7,21,60,.92),rgba(7,21,60,.55)),url(${hero})`}}><div><h1>Cahier Pro</h1><p>Demandes en attente, recherches réalisées, envoi vers devis client et sauvegardes journalières.</p><button className="primary" onClick={newFiche}><Plus/>Nouvelle demande</button></div></div>{currentUser.role==="admin"?<><div className="stats"><div><Clock/><b>{data.fiches.filter(f=>f.statut==="en_attente").length}</b><span>En attente</span></div><div><Edit3/><b>{data.fiches.filter(f=>f.statut==="en_cours").length}</b><span>En cours</span></div><div><CheckCircle2/><b>{data.fiches.filter(f=>f.statut==="realise").length}</b><span>Réalisés</span></div><div><Euro/><b>{money(data.devis.filter(d=>isSameDay(d.date)).reduce((s,d)=>s+totalDevis(d),0))}</b><span>Total devis jour</span></div></div><div className="panel"><h3>Suivi efficacité par salarié aujourd’hui</h3><div className="employee-table"><div className="employee-head"><span>Salarié</span><span>Fiches</span><span>Réalisés</span><span>Devis</span><span>Total</span></div>{stats.map(s=><div className="employee-row" key={s.user.id}><b>{s.user.nom}</b><span>{s.fiches}</span><span>{s.realise}</span><span>{s.devis}</span><span>{money(s.total)}</span></div>)}</div></div></>:<div className="dashboard-grid employee-only"><div className="panel"><h3>Mes statistiques aujourd’hui</h3>{(()=>{const f=data.fiches.filter(x=>x.creeParId===currentUser.id&&isSameDay(x.date));const d=data.devis.filter(x=>x.creeParId===currentUser.id&&isSameDay(x.date));return <div className="employee-personal-stats"><div><b>{f.length}</b><span>Mes fiches</span></div><div><b>{f.filter(x=>x.statut==="realise").length}</b><span>Réalisés</span></div><div><b>{d.length}</b><span>Mes devis</span></div><div><b>{money(d.reduce((s,x)=>s+totalDevis(x),0))}</b><span>Total</span></div></div>})()}</div><div className="panel"><h3>Mes demandes à reprendre</h3><div className="today-list">{data.fiches.filter(f=>f.creeParId===currentUser.id&&f.statut!=="realise").slice(0,8).map(f=><button key={f.id} onClick={()=>{setEditing(f);setOpenPieceId(f.pieces?.[0]?.id||"");setActive("edition")}}><span><b>{f.immatriculation||f.clientTelephone||"Sans plaque"}</b><small>{f.date} {f.heureCreation} · {f.statut}</small></span><strong>{f.pieces?.length||splitPieces(f.demandeRapide).length} pièce(s)</strong></button>)}</div></div></div>}</section>}
function Cahier({data,currentUser,selectedUserId,setSelectedUserId,search,setSearch,visibleFiches,newFiche,setPreview,canEdit,canDelete,commit,setEditing,setOpenPieceId,setActive}){return <section><Header title="Cahier Pro" subtitle={currentUser.role==="admin"?"Admin : tous les cahiers. Recherche par plaque, VIN, nom, téléphone ou référence.":"Salarié : ton cahier uniquement. Exception : retrouver une fiche d’un autre salarié par plaque."}/><div className="toolbar"><div className="search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Recherche plaque, VIN, nom, téléphone..."/></div>{currentUser.role==="admin"?<select value={selectedUserId} onChange={e=>setSelectedUserId(e.target.value)}><option value="all">Tous les cahiers</option>{data.users.map(u=><option key={u.id} value={u.id}>Cahier de {u.nom}</option>)}</select>:<div className="locked-filter">Mon cahier</div>}<button className="primary" onClick={newFiche}><Plus/>Nouvelle</button></div><div className="cards">{visibleFiches.map(f=><article className="fiche-card" key={f.id}><div className="card-top"><div><b>{f.numero}</b><small>{f.date} {f.heureCreation} · {f.creeParNom}</small></div><span className={`badge ${f.statut}`}>{f.statut==="realise"?"Réalisé":f.statut==="en_cours"?"En cours":"En attente"}</span></div><h3>{f.clientNom||"Client non renseigné"}</h3><p><Car size={16}/>{f.immatriculation||"Plaque non renseignée"} — {vehicleName(f)||"Véhicule non renseigné"}</p><div className="mini-pieces">{((f.pieces||[]).length?f.pieces:splitPieces(f.demandeRapide).map(x=>({id:x,designation:x}))).slice(0,5).map(p=><span key={p.id}>{p.designation}</span>)}</div>{currentUser.role!=="admin"&&f.creeParId!==currentUser.id&&<div className="external-warning">Fiche déjà faite par {f.creeParNom}. Consultation par plaque uniquement.</div>}<div className="actions"><button onClick={()=>setPreview(f)}><Eye/>Afficher intégralité</button>{canEdit(f)&&<button onClick={()=>{setEditing(f);setOpenPieceId(f.pieces?.[0]?.id||"");setActive("edition")}}><Edit3/>Ouvrir</button>}{canDelete()&&<button className="danger" onClick={()=>{if(confirm("Supprimer ?"))commit({...data,fiches:data.fiches.filter(x=>x.id!==f.id)})}}><Trash2/>Supprimer</button>}</div></article>)}</div></section>}
function Editor({editing,setEditing,openPieceId,setOpenPieceId,saveFiche,sendToDevis,setPreview,cancel}){const piece=(editing.pieces||[]).find(p=>p.id===openPieceId);function setField(k,v){setEditing({...editing,[k]:v})}function upPiece(id,patch){setEditing({...editing,pieces:(editing.pieces||[]).map(p=>p.id===id?{...p,...patch}:p)})}function upProp(pid,idx,patch){setEditing({...editing,pieces:(editing.pieces||[]).map(p=>{if(p.id!==pid)return p;const props=[...(p.propositions||[])];props[idx]={...(props[idx]||propEmpty(idx+1)),...patch};return{...p,propositions:props}})})}function addProp(pid){setEditing({...editing,pieces:editing.pieces.map(p=>p.id===pid?{...p,propositions:[...(p.propositions||[]),propEmpty((p.propositions||[]).length+1)]}:p)})}function remProp(pid,idx){setEditing({...editing,pieces:editing.pieces.map(p=>{if(p.id!==pid)return p;if((p.propositions||[]).length<=1){alert("Minimum une proposition.");return p}return{...p,propositions:p.propositions.filter((_,i)=>i!==idx)}})})}function img(file,pid,idx){const r=new FileReader();r.onload=()=>upProp(pid,idx,{image:r.result});r.readAsDataURL(file)}function prepare(){const names=splitPieces(editing.demandeRapide);if(!names.length)return alert("Écris la liste de pièces.");const ex=(editing.pieces||[]).map(p=>p.designation.toLowerCase());const nouvelles=names.filter(n=>!ex.includes(n.toLowerCase())).map(n=>emptyPiece(n));const pieces=[...(editing.pieces||[]),...nouvelles];setEditing({...editing,pieces,statut:"en_cours"});setOpenPieceId(pieces[0]?.id||"")}return <section><Header title="Cahier Pro — recherche" subtitle="En attente quand la demande arrive, En cours pendant la recherche, Réalisé quand terminé."/><div className="editor"><div className="panel"><h3>Informations client / véhicule</h3><div className="grid2"><label>Numéro<input value={editing.numero} onChange={e=>setField("numero",e.target.value)}/></label><label>Date<input type="date" value={editing.date} onChange={e=>setField("date",e.target.value)}/></label><label>Heure<input value={editing.heureCreation||""} onChange={e=>setField("heureCreation",e.target.value)}/></label><label>Statut<select value={editing.statut} onChange={e=>setField("statut",e.target.value)}><option value="en_attente">En attente</option><option value="en_cours">En cours</option><option value="realise">Réalisé</option></select></label><label>Nom client<input value={editing.clientNom} onChange={e=>setField("clientNom",e.target.value)}/></label><label>Téléphone<input value={editing.clientTelephone} onChange={e=>setField("clientTelephone",e.target.value)}/></label><label>Immatriculation<input value={editing.immatriculation} onChange={e=>setField("immatriculation",e.target.value.toUpperCase())}/></label><label>VIN<input maxLength="17" value={editing.vin} onChange={e=>setField("vin",e.target.value.toUpperCase().replace(/[^A-Z0-9]/g,""))}/></label><label>Marque automatique<select value={editing.marque} onChange={e=>setEditing({...editing,marque:e.target.value,modele:""})}><option value="">Sélectionner</option>{CAR_BRANDS.map(m=><option key={m}>{m}</option>)}</select></label><label>Modèle automatique<select value={editing.modele} disabled={!editing.marque} onChange={e=>setField("modele",e.target.value)}><option value="">Sélectionner</option>{(CAR_MODELS[editing.marque]||[]).map(m=><option key={m}>{m}</option>)}</select></label><label>Marque manuelle<input value={editing.marqueManuelle||""} onChange={e=>setField("marqueManuelle",e.target.value)}/></label><label>Modèle manuel<input value={editing.modeleManuel||""} onChange={e=>setField("modeleManuel",e.target.value)}/></label><label>Finition / motorisation<input value={editing.finition} onChange={e=>setField("finition",e.target.value)}/></label></div></div><div className="panel demande-client"><div className="line-title"><div><h3>Demande rapide</h3><p className="muted">Liste donnée par le client, une pièce par ligne.</p></div><button onClick={prepare}><ClipboardList/>Commencer la recherche</button></div><textarea className="big-request" value={editing.demandeRapide} onChange={e=>setField("demandeRapide",e.target.value)} placeholder={"Kit embrayage\nKit distribution\nFiltre à air\nFiltre à huile"}/></div><div className="panel"><div className="line-title"><h3>Recherche détaillée</h3><button onClick={()=>{const p=emptyPiece("");setEditing({...editing,pieces:[...(editing.pieces||[]),p],statut:"en_cours"});setOpenPieceId(p.id)}}><Plus/>Ajouter pièce</button></div><div className="request-preview">{(editing.pieces||[]).map((p,i)=><button key={p.id} className={openPieceId===p.id?"tab-on":""} onClick={()=>setOpenPieceId(p.id)}>{i+1}. {p.designation||"Pièce sans nom"}</button>)}</div>{!piece?<div className="waiting-panel">Clique sur “Commencer la recherche”.</div>:<div className="piece-box"><div className="piece-head"><b>{piece.designation}</b><button className="danger" onClick={()=>{const rest=editing.pieces.filter(p=>p.id!==piece.id);setEditing({...editing,pieces:rest});setOpenPieceId(rest[0]?.id||"")}}><Trash2/></button></div><div className="grid2"><label>Nom pièce<input value={piece.designation} onChange={e=>upPiece(piece.id,{designation:e.target.value})}/></label><label>Quantité<input type="number" value={piece.quantite||1} onChange={e=>upPiece(piece.id,{quantite:e.target.value})}/></label></div><div className="line-title proposition-toolbar"><div><h4>Propositions</h4><small>Prix en TTC. Les références ne sortent pas sur le devis client.</small></div><button onClick={()=>addProp(piece.id)}><Plus/>Ajouter proposition</button></div><div className="two-proposals">{(piece.propositions||[]).map((pr,idx)=><div className={`simple-proposal ${pr.selectionnee?"selected-proposal":""}`} key={pr.id}><div className="proposal-head"><b>Proposition {idx+1}</b><div className="proposal-head-actions"><label className="radio-choice"><input type="checkbox" checked={!!pr.selectionnee} onChange={e=>upProp(piece.id,idx,{selectionnee:e.target.checked})}/>Sélectionner</label>{(piece.propositions||[]).length>1&&<button className="danger" onClick={()=>remProp(piece.id,idx)}><Trash2/>Supprimer</button>}</div></div><div className="grid2"><label>Référence<input value={pr.reference||""} onChange={e=>upProp(piece.id,idx,{reference:e.target.value})}/></label><label>Marque / fournisseur<input value={pr.marque||""} onChange={e=>upProp(piece.id,idx,{marque:e.target.value})}/></label><label>Prix TTC<input type="number" value={pr.prix||""} onChange={e=>upProp(piece.id,idx,{prix:e.target.value})}/></label><label>Note<input value={pr.note||""} onChange={e=>upProp(piece.id,idx,{note:e.target.value})}/></label></div><div className="image-line proposition-image-line">{pr.image?<img src={pr.image}/>:<div className="empty-img"><ImagePlus/>Image réf.</div>}<label className="upload"><ImagePlus/>Ajouter image<input type="file" accept="image/*" onChange={e=>e.target.files?.[0]&&img(e.target.files[0],piece.id,idx)}/></label>{pr.image&&<button className="danger" onClick={()=>upProp(piece.id,idx,{image:""})}><Trash2/>Retirer</button>}</div></div>)}</div></div>}</div><div className="bottom-actions"><button onClick={cancel}><X/>Retour</button><button onClick={()=>setPreview(editing)}><Eye/>Afficher l'intégralité du devis</button><button className="primary" onClick={()=>saveFiche({...editing,statut:editing.statut==="en_attente"?"en_cours":editing.statut})}><Save/>Enregistrer dans le cahier</button><button onClick={()=>sendToDevis({...editing,statut:"realise"})}><Send/>Envoyer vers devis</button></div></div></section>}
function DevisView({visibleDevis,search,setSearch}){return <section><Header title="Devis" subtitle="Devis client imprimable, sans références internes."/><div className="toolbar"><div className="search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Recherche plaque, VIN, nom..."/></div></div><div className="cards">{visibleDevis.map(d=><article className="fiche-card" key={d.id}><div className="card-top"><div><b>{d.numero}</b><small>{d.date} {d.heureCreation} · {d.creeParNom}</small></div><span className="badge realise">Devis client</span></div><h3>{d.clientNom||"Client non renseigné"}</h3><p><Car size={16}/>{d.immatriculation||"Sans plaque"} — {d.vehicule}</p><div className="mini-pieces">{(d.lignes||[]).slice(0,5).map(l=><span key={l.id}>{l.designation} · {money(l.prixTTC)}</span>)}</div><div className="actions"><button onClick={()=>printDevisClient(d)}><Printer/>Imprimer devis</button></div></article>)}</div></section>}
function Sauvegardes({data,commit}){return <section><Header title="Sauvegardes journalières" subtitle="Clôture du jour à 19h si le site est ouvert. Tu peux aussi clôturer manuellement."/><div className="actions top-actions"><button className="primary" onClick={()=>commit(closeDay(data,today()))}><Save/>Sauvegarder aujourd’hui</button></div><div className="cards">{(data.archivesJour||[]).map(a=><article className="fiche-card" key={a.id}><h3>Sauvegarde du {a.date}</h3><p>{a.fiches.length} fiche(s) · {a.devis.length} devis</p><div className="mini-pieces">{a.resume.map(r=><span key={r.userId}>{r.nom} : {r.fiches} fiches / {r.devis} devis</span>)}</div><button onClick={()=>printArchiveJour(a)}><Printer/>Imprimer archive jour</button></article>)}</div></section>}
function UsersView({data,commit,currentUser,userForm,setUserForm}){return <section><Header title="Utilisateurs" subtitle="Créer/supprimer les comptes salariés."/><div className="user-grid"><div className="panel"><h3>Créer utilisateur</h3><input placeholder="Nom" value={userForm.nom} onChange={e=>setUserForm({...userForm,nom:e.target.value})}/><input placeholder="Identifiant" value={userForm.identifiant} onChange={e=>setUserForm({...userForm,identifiant:e.target.value})}/><input placeholder="Mot de passe" value={userForm.motDePasse} onChange={e=>setUserForm({...userForm,motDePasse:e.target.value})}/><select value={userForm.role} onChange={e=>setUserForm({...userForm,role:e.target.value})}><option value="salarie">Salarié</option><option value="admin">Admin</option></select><button className="primary" onClick={()=>{if(!userForm.nom||!userForm.identifiant||!userForm.motDePasse)return alert("Remplis tout.");commit({...data,users:[...data.users,{...userForm,id:uid()}]});setUserForm({nom:"",identifiant:"",motDePasse:"",role:"salarie"})}}><Plus/>Ajouter</button></div><div className="panel"><h3>Liste</h3>{data.users.map(u=><div className="user-row" key={u.id}><div><b>{u.nom}</b><small>{u.identifiant} · {u.role} · mot de passe : {u.motDePasse}</small></div>{u.id!==currentUser.id&&<button className="danger" onClick={()=>{if(confirm("Supprimer ?"))commit({...data,users:data.users.filter(x=>x.id!==u.id)})}}><Trash2/>Supprimer</button>}</div>)}</div></div></section>}
function PreviewModal({fiche,onClose,sendToDevis}){return <div className="modal-back"><div className="modal"><div className="line-title"><h2>Aperçu intégral — {fiche.numero}</h2><button onClick={onClose}><X/></button></div><div className="preview-info"><b>{fiche.clientNom||"Client non renseigné"}</b><span>{fiche.immatriculation||"Sans plaque"} · {vehicleName(fiche)} · {fiche.statut}</span></div><table className="preview-table"><thead><tr><th>Pièce</th><th>Qté</th><th>Propositions sélectionnées</th><th>Total TTC</th></tr></thead><tbody>{(fiche.pieces||[]).map(p=><tr key={p.id}><td>{p.designation}</td><td>{p.quantite||1}</td><td>{selectedProps(p).map((pr,i)=><div key={pr.id}>Prop. {i+1} : {pr.reference} · {pr.marque} · {money(pr.prix)}</div>)}</td><td>{money(selectedProps(p).reduce((s,pr)=>s+Number(pr.prix||0)*Number(p.quantite||1),0))}</td></tr>)}</tbody></table><div className="preview-total">Total sélectionné : {money(totalFiche(fiche))}</div><div className="actions"><button onClick={sendToDevis}><Send/>Envoyer vers devis</button><button onClick={()=>printFicheTechnique(fiche)}><Printer/>Imprimer fiche interne</button></div></div></div>}
function printFicheTechnique(){window.print()}
function printDevisClient(d){const rows=(d.lignes||[]).map((l,i)=>{const total=Number(l.quantite||1)*Number(l.prixTTC||0);return`<tr><td>${i+1}</td><td>${l.designation||""}</td><td>${l.quantite||1}</td><td>${money(htFromTtc(l.prixTTC))}</td><td>${money(tvaFromTtc(l.prixTTC))}</td><td>${money(l.prixTTC)}</td><td>${money(total)}</td></tr>`}).join("");const totalTTC=totalDevis(d);const w=window.open("","_blank");w.document.write(`<html><head><title>${d.numero}</title><style>@page{size:A4;margin:12mm}body{font-family:Arial;color:#111827}.top{display:flex;justify-content:space-between;border-bottom:3px solid #d4a21c;padding-bottom:14px}.brand{display:flex;gap:12px;align-items:center}.brand img{width:80px}h1{color:#071b4b;margin:0}.info{text-align:right;font-size:13px;line-height:1.6}.grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.box{border:1px solid #d8def0;border-radius:12px;padding:10px;margin:12px 0}table{width:100%;border-collapse:collapse;margin-top:16px;font-size:13px}th{background:#071b4b;color:white;padding:9px;text-align:left}td{border:1px solid #d8def0;padding:8px}.totals{margin-top:16px;margin-left:auto;width:320px}.totals div{display:flex;justify-content:space-between;border-bottom:1px solid #ddd;padding:8px}.grand{font-size:20px;font-weight:900;color:#000}.footer{position:fixed;bottom:10mm;left:12mm;right:12mm;text-align:center;border-top:1px solid #d4a21c;padding-top:8px;font-size:12px}</style></head><body><div class="top"><div class="brand"><img src="${logo}"><div><h1>${ENTREPRISE.nom}</h1><b>${ENTREPRISE.slogan}</b></div></div><div class="info">📞 ${ENTREPRISE.telFixe}<br>🟢 ${ENTREPRISE.whatsapp}<br>✉️ ${ENTREPRISE.email}<br>📍 ${ENTREPRISE.adresse}</div></div><h2>DEVIS CLIENT — ${d.numero}</h2><div class="grid"><div class="box"><b>Client</b><br>${d.clientNom||""}</div><div class="box"><b>Téléphone</b><br>${d.clientTelephone||""}</div><div class="box"><b>Immatriculation</b><br>${d.immatriculation||""}</div><div class="box"><b>Véhicule</b><br>${d.vehicule||""}</div></div><table><thead><tr><th>N°</th><th>Désignation</th><th>Qté</th><th>Prix HT</th><th>TVA</th><th>Prix TTC</th><th>Total TTC</th></tr></thead><tbody>${rows}</tbody></table><div class="totals"><div><span>Total HT</span><b>${money(htFromTtc(totalTTC))}</b></div><div><span>TVA 20%</span><b>${money(tvaFromTtc(totalTTC))}</b></div><div class="grand"><span>Total TTC</span><b>${money(totalTTC)}</b></div></div><div class="footer">${ENTREPRISE.nom} — ${ENTREPRISE.adresse} — ${ENTREPRISE.email}</div><script>window.print()</script></body></html>`);w.document.close()}
function printArchiveJour(a){const rows=(a.resume||[]).map(r=>`<tr><td>${r.nom}</td><td>${r.fiches}</td><td>${r.devis}</td><td>${money(r.total)}</td></tr>`).join("");const w=window.open("","_blank");w.document.write(`<html><body><h1>Archive journée ${a.date}</h1><table border="1" cellpadding="8"><tr><th>Salarié</th><th>Fiches</th><th>Devis</th><th>Total</th></tr>${rows}</table><script>window.print()</script></body></html>`);w.document.close()}
createRoot(document.getElementById("root")).render(<App/>);
